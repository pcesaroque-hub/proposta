
import { PricingData, CalculationResult } from "../types";

// Serviço de IA desativado.
// A lógica foi removida para eliminar a dependência do @google/genai.

export async function generateProposalJustification(data: PricingData, result: CalculationResult) {
  return "A geração de justificativa por IA foi desativada.";
}
